# Deploy config (use another branch):
# 1) change $fa-font-path in app/styles/main.scss
# 2) run `grunt build`
# 3) uncomment in .gitignore the `dist` directory (heroku needs it)
# 4) run `git push heroku feature/angular:master`
web: node web.js
