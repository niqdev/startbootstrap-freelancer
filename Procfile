# Deploy config (use another branch):
# - change $fa-font-path in app/styles/main.scss
# - run `grunt build`
# - uncomment in .gitignore the 'dist' directory (heroku needs it)
# - run `git push heroku feature/angular:master`
web: node web.js
