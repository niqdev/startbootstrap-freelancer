# Deploy config (use another branch):
# 1) change $fa-font-path in app/styles/main.scss
# 2) add task `cdnify` in Gruntfile.js
# 3) run `grunt build`
# 4) uncomment in .gitignore the `dist` directory (heroku needs it)
# 5) commit and run `git push heroku feature/angular:master`
web: node web.js
